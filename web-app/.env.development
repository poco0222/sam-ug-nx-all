# 开发态默认开启手输模式：允许输入制造令号/零件名称并按刷新查询。
VITE_PARTS_ORDER_DEV_MANUAL_MODE=true
# 开发态后端地址：用于零件工单及字典接口请求（含 /system/dict/data/type/*）。
# 当前默认指向正式服务器，便于与 Qt 宿主联调时保持一致。
VITE_API_BASE_URL=http://192.168.19.100:8080
